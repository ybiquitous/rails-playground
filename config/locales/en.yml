en:
  players:
    create:
      success: Player was created!
    update:
      success: Player was updated!
    destroy:
      success: Player was destroyed!

  confirm:
    destroy: Are you sure?

  new: "New %{model}"
  back: Back
  edit: Edit
  editing: "Editing %{model}"

  id: ID

  activerecord:
    models:
      player:
        one: Player
        other: Players
