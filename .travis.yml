language: ruby

cache:
  bundler: true
  yarn: true
  directories:
    - node_modules

addons:
  apt:
    packages:
      - graphviz

env:
  - DATABASE_SOCKET=

install:
  - mysql --version
  - bundle install --jobs=3 --retry=3
  - nvm install
  - yarn install

before_script:
  - bin/rails db:setup

script:
  - bin/rails test
  - yarn lint

deploy:
  provider: script
  script: rvm . do .travis/deploy.sh
  on:
    branch: master

notifications:
  email: false
